# VulnCraft test fixture — INTENTIONALLY OUTDATED dependencies with known CVEs.
# Used to validate the dependency-audit (pip-audit) stage. Do NOT merge.
Flask==0.12.2
Django==1.11.0
requests==2.6.0
PyYAML==3.12
Jinja2==2.7.2
urllib3==1.24.1
cryptography==2.3
